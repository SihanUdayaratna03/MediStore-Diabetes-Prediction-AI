/**
 * DocViewer.jsx
 * TODO: Implement the uploaded document viewer component.
 *
 * Props:
 *   sessionData  — UploadResponse object (session_id, filename, doc_type, total_pages,
 *                  chunk_count, preview_text, extraction_method)
 *
 * Features:
 *   - Document info card (filename, page count, chunk count, extraction method)
 *   - Collapsible text preview (first 500 chars of extracted text)
 *   - "Document ready — ask questions in the chat" hint
 *   - Different icon for PDF vs image doc_type
 */
